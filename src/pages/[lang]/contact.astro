---
import { languages, ui } from "../../i18n/ui";
import Layout from "../../layouts/Layout.astro";
import Hero from "../../components/contact/Hero.astro";
import Form from "../../components/contact/Form.astro";
import Info from "../../components/contact/Info.astro";

export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
    { params: { lang: "es" } },
    { params: { lang: "de" } },
    { params: { lang: "it" } },
    { params: { lang: "pt" } },
  ];
}

const { lang } = Astro.params;
const t = ui[lang as keyof typeof ui] || ui.es;
const contact = t.contact;
---

<Layout
  lang={lang}
  title={`uDefine Consulting | ${languages[lang as keyof typeof languages]?.label || "Contact Us"}`}
  description={languages[lang as keyof typeof languages]?.description ||
    "Desarrollo experto en SAP Fiori y consultoría estratégica. uDefine potencia tu empresa con UX moderna, migraciones a S/4HANA y soluciones cloud."}
>
  <main class="flex flex-col min-h-screen pt-16">
    <Hero
      badge={contact.badge}
      title1={contact.hero.title1}
      titleHighlight={contact.hero.titleHighlight}
      subtitle={contact.hero.subtitle}
    />

    <section
      class="py-20 px-4 lg:px-40 bg-surface-dark border-t border-white/5 relative"
    >
      <div class="layout-content-container mx-auto max-w-[1200px]">
        <div class="grid lg:grid-cols-2 gap-16 lg:gap-24">
          <Form {...contact.form} />
          <Info {...contact.info} />
        </div>
      </div>
    </section>
  </main>
</Layout>
